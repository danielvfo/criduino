<!DOCTYPE html>
<html>
<head>
  <title>Criduino</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header class="navbar navbar-fixed-top navbar-default">
    <div class="container">
      <%= link_to "CRIduino", "", class: "btn btn-lg btn-success", id: "logo"%>
      <div class="nav_item">
        <% if user_signed_in? %>
            Signed in as <%= current_user.email %>. Not you?
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-default navbar-btn" %>
        <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "btn btn-default navbar-btn" %> or
            <%= link_to "Sign in", new_user_session_path, class: "btn btn-default navbar-btn" %>
        <% end %>
      </div>
    </div>
  </header>
<%= yield %>
</body>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
</html>
